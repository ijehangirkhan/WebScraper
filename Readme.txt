Libraries used:
1) Regex Library ( re ):
                        This library is used to filter out the irrelevant data like Html tags etc and extract the pure text from website.
2) Requests ( requests):
                        This Library is used to request the URL using HTTP.


How to Setup and Execute:
                        This project can be executed in using any Python Editor like Pycharm etc.
You need to install any latest version of python first.